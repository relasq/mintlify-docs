---
title: "Listar bots"
description: "Retorna todos os bots do usuário"
openapi: GET /bots
---

Retorna todos os bots do usuário autenticado.

## Exemplo cURL

```bash
curl -X GET "https://api.stackr.lat/bots" \
  -H "Authorization: Bearer SEU_API_TOKEN"
```

## Resposta

```json
[
  {
    "id": "123e4567-e89b-12d3-a456-426614174000",
    "name": "Bot1",
    "status": "RUNNING",
    "memoryMb": 256,
    "ram": "50MB / 256MB"
  }
]
```
