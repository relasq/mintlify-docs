---
title: "Criar pasta"
description: "Cria uma nova pasta no sistema de arquivos do bot"
openapi: POST /bots/{id}/files/mkdir
---

Cria uma nova pasta no sistema de arquivos do bot.

## Parâmetros de path

| Parâmetro | Tipo | Descrição |
|-----------|------|-----------|
| `id` | string | ID único do bot |

## Body (JSON)

| Campo | Tipo | Obrigatório | Descrição |
|-------|------|-------------|-----------|
| `path` | string | Sim | Caminho da nova pasta |

## Exemplo cURL

```bash
curl -X POST "https://api.stackr.lat/bots/bot_abc123/files/mkdir" \
  -H "Authorization: Bearer SEU_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"path": "/app/data"}'
```

## Resposta

```json
{
  "success": true
}
```
